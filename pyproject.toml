[project]
name = "test-arkose"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
    "websocket-client>=1.9.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
